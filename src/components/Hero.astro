---
import ShinyText from "./text/Gradients"
---
<section class="absolute inset-0 flex flex-col items-center justify-center text-white text-center pointer-events-none" id="home">

    <div class="mt-[100px] md:mt-[100px] relative z-10">
        <div class="inline-block">
            <span class="text-gray-200 bg-white/10 rounded-full px-4 p-2 bg-opacity-40 text-sm md:text-2xl mb-4 md:mb-6 block">
                Impulsamos tu negocio en la era digital
            </span>
        </div>

        <h1 class="px-4 lg:px-32 text-[2rem] md:text-7xl font-bold leading-tight max-w-6xl mx-auto relative">
            <!-- Mobile background decoration - only visible on mobile -->
            <div class="absolute inset-0 md:hidden -z-10">
                <!-- Animated gradient blobs -->
                <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[150%] opacity-30 blur-2xl bg-gradient-to-r from-teal-400 via-cyan-500 to-blue-500 animate-pulse"></div>
                <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[110%] h-[140%] opacity-20 blur-3xl bg-gradient-to-br from-purple-500 via-teal-700 to-teal-400 animate-pulse" style="animation-delay: 0.5s; animation-duration: 3s;"></div>
            </div>

            <!-- Glassmorphism background for text - only on mobile -->
            <span class="md:hidden absolute inset-0 -z-10"></span>

            <!-- Text content with mobile-optimized UX/UI -->
            <span class="text-teal-300 bg-clip-text relative z-10 font-bold
                         max-md:text-[2.15rem] max-md:leading-[1.15] max-md:tracking-tight
                         max-md:drop-shadow-[0_2px_8px_rgba(20,184,166,0.4)]
                         max-md:block max-md:mb-1.5">
            EXPERIENCIAS DIGITALES
            </span>
            <span class="relative z-10 font-bold
                         max-md:text-[2.15rem] max-md:leading-[1.15] max-md:tracking-normal
                         max-md:drop-shadow-[0_2px_12px_rgba(255,255,255,0.6)]
                         max-md:block max-md:mt-1.5 max-md:font-extrabold
                         max-md:text-white/95"> QUE  INSPIRAN</span>
        </h1>
    </div>

    <!-- Bottom overlay for smooth transition to next section -->
    <div class="absolute bottom-0 left-0 right-0 h-32 md:h-40 bg-gradient-to-b from-transparent to-black pointer-events-none z-20"></div>
</section>
