
<section class="absolute inset-0 flex flex-col items-center justify-center text-white text-center pointer-events-none" id="home" style="contain: layout style;">

    <div class="relative z-10" style="padding-top: 70px; min-height: 200px;">
    <style>
        @media (min-width: 768px) {
            #home > div {
                padding-top: 100px !important;
            }
        }
    </style>
        <div class="inline-block">
            <span class="inline-flex items-center gap-2 px-3 py-1 rounded-full text-xs md:text-sm font-medium mb-4 md:mb-6"
                 style="background: rgba(76, 214, 192, 0.1); border: 1px solid rgba(76, 214, 192, 0.2); color: rgba(76, 214, 192, 0.9);">
                Desarrollo y Diseño Web Profesional
            </span>
        </div>

        <h1 class="px-4 lg:px-32 text-[2rem] md:text-7xl font-bold leading-tight max-w-6xl mx-auto relative" style="content-visibility: auto;">
            <!-- Mobile background decoration - only visible on mobile -->
            <div class="absolute inset-0 md:hidden -z-10" style="content-visibility: auto; contain: layout style paint;">
                <!-- Animated gradient blobs - deferred rendering -->
                <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[150%] opacity-30 blur-2xl bg-gradient-to-r from-teal-400 via-cyan-500 to-blue-500 animate-pulse" style="will-change: opacity; transform: translateZ(0);"></div>
                <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[110%] h-[140%] opacity-20 blur-3xl bg-gradient-to-br from-purple-500 via-teal-700 to-teal-400 animate-pulse" style="animation-delay: 0.5s; animation-duration: 3s; will-change: opacity; transform: translateZ(0);"></div>
            </div>

            <!-- Glassmorphism background for text - only on mobile -->
            <span class="md:hidden absolute inset-0 -z-10"></span>

            <!-- Text content with mobile-optimized UX/UI -->
            <span class="text-teal-300 bg-clip-text relative z-10 font-bold
                         max-md:text-[2.15rem] max-md:leading-[1.15] max-md:tracking-tight
                         max-md:drop-shadow-[0_2px_8px_rgba(20,184,166,0.4)]
                         max-md:block max-md:mb-1.5">
            Experiencias Digitales
            </span>
            <span class="relative z-10 font-bold
                         max-md:text-[2.15rem] max-md:leading-[1.15] max-md:tracking-normal
                         max-md:drop-shadow-[0_2px_12px_rgba(255,255,255,0.6)]
                         max-md:block max-md:mt-1.5 max-md:font-extrabold
                         max-md:text-white"> que inspiran</span>
        </h1>
    </div>

    <!-- Bottom overlay for smooth transition to next section -->
    <div class="absolute bottom-0 left-0 right-0 h-32 md:h-40 bg-gradient-to-b from-transparent to-black pointer-events-none z-10"></div>
</section>
